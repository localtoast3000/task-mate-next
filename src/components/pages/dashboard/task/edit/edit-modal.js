import TaskFormModal from '@/components/pages/dashboard/task-form-modal/task-form-modal';

export default function EditModal({ onCloseClick }) {
  return <TaskFormModal onCloseClick={onCloseClick} />;
}
